/// @description Insert description here
// You can write your code in this editor
x = round(obj_camera.viewx + xx - global.gameWidth/2);
y = round(obj_camera.viewy + yy - global.gameHeight/2);

if keyboard_check_direct(ativation_key) and bar obj_inventory.current = inventory_slot;